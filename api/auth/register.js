
const bcrypt=require('bcryptjs'); const crypto=require('crypto'); const {getRedis}=require('../_lib/shared');
module.exports=async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const redis=getRedis(); if(!redis) return res.status(500).json({error:'База данных не подключена'}); const {email,password,name}=req.body||{}; if(!email||!password) return res.status(400).json({error:'Email и пароль обязательны'}); if(String(password).length<6) return res.status(400).json({error:'Пароль слишком короткий'}); const key=String(email).toLowerCase().trim(); try{ if(await redis.get(`user-by-email:${key}`)) return res.status(400).json({error:'Такой email уже зарегистрирован'}); const userId=crypto.randomUUID(); const user={id:userId,email:key,name:name||key.split('@')[0],passwordHash:await bcrypt.hash(password,10),createdAt:Date.now()}; await redis.set(`user:${userId}`,user); await redis.set(`user-by-email:${key}`,userId); const sid=crypto.randomUUID(); const TTL=2592000; await redis.set(`session:${sid}`,userId,{ex:TTL}); res.setHeader('Set-Cookie',`session=${sid}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=${TTL}`); return res.status(200).json({user:{email:user.email,name:user.name}});}catch(e){return res.status(500).json({error:'Ошибка БД: '+e.message})} }
