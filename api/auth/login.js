
const bcrypt=require('bcryptjs'); const crypto=require('crypto'); const {getRedis}=require('../_lib/shared');
module.exports=async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const redis=getRedis(); if(!redis) return res.status(500).json({error:'База данных не подключена'}); const {email,password}=req.body||{}; if(!email||!password) return res.status(400).json({error:'Email и пароль обязательны'}); const key=String(email).toLowerCase().trim(); try{ const userId=await redis.get(`user-by-email:${key}`); if(!userId) return res.status(401).json({error:'Неверный email или пароль'}); const user=await redis.get(`user:${userId}`); if(!user) return res.status(401).json({error:'Неверный email или пароль'}); const ok=await bcrypt.compare(password,user.passwordHash); if(!ok) return res.status(401).json({error:'Неверный email или пароль'}); const sid=crypto.randomUUID(); const TTL=2592000; await redis.set(`session:${sid}`,userId,{ex:TTL}); res.setHeader('Set-Cookie',`session=${sid}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=${TTL}`); return res.status(200).json({user:{email:user.email,name:user.name}});}catch(e){return res.status(500).json({error:'Ошибка БД: '+e.message})} }
