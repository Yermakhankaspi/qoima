const {getRedis,parseCookies}=require('../_lib/shared'); module.exports=async function handler(req,res){try{const s=parseCookies(req.headers.cookie).session; const redis=getRedis(); if(redis&&s) await redis.del(`session:${s}`)}catch(e){} res.setHeader('Set-Cookie','session=; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=0'); return res.status(200).json({ok:true})}
